{
    "configurations": [
        {
            "name": "Run the ising model",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/ising.exe",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "Compile main file"
        }
    ],
    "version": "2.0.0"
}